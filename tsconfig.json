{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "baseUrl": "src", // Set the base URL to "src"
    "strict": true,
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
